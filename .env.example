# Owner WhatsApp number — digits only, no +, no leading 0.
OWNER_PHONE=923202067666

# Admin panel password
ADMIN_PASSWORD=admin123

# Upstash Redis credentials (from console.upstash.com or the Vercel integration).
# Leave blank locally to use JSON files instead of Redis.
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=

PORT=3001
